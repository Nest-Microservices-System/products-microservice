Products Microservice 

## Dev

1. Clone repository
2. Install Dependencies
3. Create a `.env` file based on the `.env.template`
4. Execute prisma migration: ` npx prisma migrate dev`
5. Start the application: `yarn start:dev` or `npm run start:dev`